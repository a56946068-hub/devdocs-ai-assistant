# DevDocs AI Assistant
Open-source CLI tool for semantic documentation search using Claude.

## Setup
1. `pip install -r requirements.txt`
2. Update API key in `src/engine.py`
3. Use `python src/engine.py ask "Your question"`